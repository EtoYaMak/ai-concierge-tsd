{
  "version": 2,
  "buildCommand": "vite build --outDir dist/public && esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=api",
  "outputDirectory": "dist/public",
  "installCommand": "npm install",
  "framework": null,
  "functions": {
    "dist/index.js": {
      "memory": 1024,
      "maxDuration": 60
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/" }
  ]
}
